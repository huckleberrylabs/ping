import Company from "./index";
/* 
describe("CandianLawList:Company", () => {
  test("Should Extract", async () => {
    expect.assertions(1);
    const extracted = await Company.extract(
      "https://canadianlawlist.com/listingdetail/company/beard-winter-llp-688641/"
    );
    const normalized = await Company.normalize(extracted);
    const validated = await Company.schema.validate(normalized);
    expect(validated).toBeTruthy();
  });
});
 */
