import Contact from "./index";

/* const URL =
  "https://canadianlawlist.com/listingdetail/contact/charles-a-boulakia-599052/";

describe("CandianLawList:Contact", () => {
  test("Should Extract", async () => {
    expect.assertions(1);
    const extracted = await Contact.extract(URL);
    const normalized = await Contact.normalize(extracted);
    const validated = await Contact.schema.validate(normalized);
    expect(validated).toBeTruthy();
  });
});
 */
