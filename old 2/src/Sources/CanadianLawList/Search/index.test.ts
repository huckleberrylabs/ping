import Search from "./index";
/* 
describe("index", () => {
  test(
    "Should Return an Array",
    async () => {
      expect.assertions(1);
      const result = await Search.index();
      expect(Array.isArray(result)).toBeTruthy();
    },
    15000
  );
});

const URL =
  "https://canadianlawlist.com/searchresult/?searchtype=lawyers&province=on";

describe("Extract Search", () => {
  test("Should Return 10 Tasks", async () => {
    expect.assertions(1);
    const result = await Search.process({
      url: URL,
    });
    expect(result.length === 10).toBeTruthy();
  });
});
 */
