{
  "name": "@huckleberryai/monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "lerna clean -y && lerna bootstrap",
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test  --stream"
  },
  "devDependencies": {
    "lerna": "^3.18.1"
  },
  "dependencies": {
    "papaparse": "^5.1.1"
  }
}
