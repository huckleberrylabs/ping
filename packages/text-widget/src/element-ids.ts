import { UUID, IUUID } from "@huckleberryai/core";

export const INSERT_SCRIPT_ID: IUUID = "huckleberry-text-insert-script";
export const CSS_ID: IUUID = `css-${UUID()}`;

export const CONTAINER_ID: IUUID = `container-${UUID()}`;
export const FORM_ID: IUUID = `form-${UUID()}`;
export const OPEN_BUTON_ID: IUUID = `open-button-${UUID()}`;
export const OPEN_BUTON_ICON_ID: IUUID = `open-button-icon-${UUID()}`;

export const MESSAGE_INPUT_ID: IUUID = `message-input-${UUID()}`;
export const MESSAGE_BUTTON_ID: IUUID = `message-button-${UUID()}`;
export const PHONE_INPUT_ID: IUUID = `phone-input-${UUID()}`;
export const PHONE_BUTTON_ID: IUUID = `phone-button-${UUID()}`;
export const NAME_INPUT_ID: IUUID = `name-input-${UUID()}`;
export const SEND_BUTTON_ID: IUUID = `send-button-${UUID()}`;

export const LOADER_ID: IUUID = `loader-${UUID()}`;
export const SUCCESS_ID: IUUID = `success-${UUID()}`;
export const ERROR_ID: IUUID = `error-${UUID()}`;
