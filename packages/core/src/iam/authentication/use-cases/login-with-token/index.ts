import * as Command from "./command";
import Handler from "./handler";
import Controller from "./controller";

const Route = "/v1/auth/login-with-token";

export { Route, Command, Handler, Controller };
